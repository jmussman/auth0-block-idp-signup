{
  "name": "auth0-block-idp-signup",
  "version": "1.0.0",
  "lockfileVersion": 3,
  "requires": true,
  "type": "module",
  "packages": {},
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "auth0": "^4.8.0",
    "ejs-lint": "^2.0.0",
    "vitest": "latest",
    "vitest-mock-commonjs": "^1.0.1"
  },
  "scripts": {
    "test": "vitest run --no-file-parallelism",
    "test-coverage": "vitest run --no-file-parallelism --coverage"
  }
}
